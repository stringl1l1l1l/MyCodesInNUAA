#include <bits/stdc++.h>
using namespace std;

// 此题的旋转数组有一些特殊的性质
// 1.将该数组分为任意两半，一定有一半是有序的
// 2.判断该数组的子数组是否有序，只要看子数组首尾的大小关系
class Solution
{
public:
    int search(vector<int> &nums, int target)
    {
        int n = nums.size();
    }
};