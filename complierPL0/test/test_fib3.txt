program p;
const a:=1;
var n, res, i;
    procedure fib(n,n,n,n);
    var sum;
    begin
        sum := 0;
        if n<2 then
            res := 1
        else
        begin
            call fib(a,n-1);
            sum := sum + res;
            call fib(a,n-2);
            sum := sum + res;
            res := sum
        end
    end
begin
    call fib(1,2,3,10);
    write(res)
end