program p;
const a:=1;
var n, res, i;
    procedure fib(n);
    var sum;
    begin
        sum := 0;
        if n<2 then
            res := 1
        else
        begin
            call fib(n-1);
            sum := sum + res;
            call fib(n-2);
            sum := sum + res;
            res := sum
        end
    end
begin
    read(n);
    while i<=n do
    begin
        call fib(i);
        write(res);
        i := i+1
    end
end